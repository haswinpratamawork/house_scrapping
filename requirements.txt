# Runtime dependencies for the scraper + API service (pinned).
# Install: pip install -r requirements.txt
# Dev/test tools (pytest, ruff) live in pyproject.toml's [dev] extra.
httpx==0.28.1
pydantic==2.13.4
tenacity==9.1.4
python-dotenv==1.2.2
selectolax==0.4.10
psycopg[binary]==3.3.4
fastapi==0.136.3
uvicorn[standard]==0.49.0
